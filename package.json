{
  "name": "frontend-react-hybrid",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "turbo dev",
    "build": "turbo build",
    "clean": "turbo clean && rm -rf node_modules",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.7"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.5.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
